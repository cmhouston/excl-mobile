// Global styles

".windowHeader": {
	width: Ti.UI.FILL,
	height: "50dip",
	backgroundColor: Alloy.Globals.colors.navBarColor
}

".windowHeader[formFactor=tablet]":{
	height: "75dip"
}

// Keep height sizing in dip. If that changes, change exhibitLanding makeDefaultUnitsFromDip() calls
".headingRow": {
	width: Ti.UI.FILL,
	height: '50 dip'
}

".headingText": {
	color: Alloy.Globals.colors.lightFontColor,
	font: {
		fontSize: '22dip',
		fontWeight: 'bold',
		fontFamily: Alloy.Globals.defaultGlobalFontFamily
	},
	textAlign: 'center',
	wordWrap: false,
	ellipsize: true,
}

".headingText[formFactor=tablet]":{
	color: Alloy.Globals.colors.lightFontColor,
	font:{
		fontWeight: 'bold',
		fontSize: "40dip",
		fontFamily: Alloy.Globals.defaultGlobalFontFamily
	},
	textAlign: 'center',
	wordWrap: false,
	ellipsize: true,
}

"Label": {
	font: {
		fontFamily: Alloy.Globals.defaultGlobalFontFamily
	},
}
