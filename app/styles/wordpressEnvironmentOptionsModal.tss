
".container": {}

".verticalContainer": {
	layout: "vertical",
	backgroundColor: "white"
}

".environmentOption": {
	width: "70%",
	font: {
		fontSize: "20dip",
		fontFamily: Alloy.CFG.excl.defaultGlobalFontFamily
	}
}

".boldLabel":{
	left: "5%",
	color: "#000000",
	font:
	{
		fontWeight: "bold",
		color: "#000000",
		fontFamily: Alloy.CFG.excl.defaultGlobalFontFamily
	}
}

"#currentEnvironLabel":{
	left: "5%",
	color: "#000000",
	font: {
		fontSize: "15dip",
		color: "#000000",
		fontFamily: Alloy.CFG.excl.defaultGlobalFontFamily
	}
}

"#toggleUnpublishedPostsSwitch":{
	left: "5%"
}

".horizontalContainer": {
	top: "5%",
	layout: "horizontal",
	height: Ti.UI.SIZE
}

".title":{
	top: "5%",
	bottom: "5%",
	color: "#000000",
	font:
	{		
		fontSize: "15dip",
		fontWeight: "bold",
		color: "#000000",
		fontFamily: Alloy.CFG.excl.defaultGlobalFontFamily
	},
	height: Ti.UI.SIZE
}
